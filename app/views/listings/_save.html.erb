<%= form_with(model: current_user.saved_listings.build, url: save_url, local: true) do |f| %>
<div><%= hidden_field_tag :listing_id, @listing.id %></div>
<%= f.submit "Save", class: "btn btn-primary btn-sm" %>
<% end %>
