<div class="text-center mt-5">
  <h1>Profile</h1>
  <p class="lead mt-2">First Name: <%= @current_user.first_name %>,<p>
  <b class="lead mt-2">Last Name: <%= @current_user.last_name %>,<p>
  <p class="lead mt-2">Email: <%= @current_user.email %><p> 
  <p class="lead mt-2">Bio: <%= @current_user.bio %></p>
</div>

<div class="text-center mt-2">
  <%= link_to "Edit", edit_user_path(current_user)%><br>
</div>

<%= render @listings  %>
